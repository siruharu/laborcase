# Build context 에 포함되지 않을 경로.
# Dockerfile 이 frontend/ 안의 소스만 필요로 하고, 결과 (build/cache) 는
# 컨테이너 안에서 다시 만든다.

node_modules
.next
out
build

# 테스트 / e2e 산출물.
test-results
playwright-report
playwright/.cache
tests/e2e/__captures__

# 환경설정 — 실 secret 은 절대 이미지에 들어가면 안 됨.
.env
.env.*
!.env.example

# 에디터 / OS noise.
.DS_Store
*.swp
.idea
.vscode

# Git / CI metadata.
.git
.gitignore
.dockerignore
README.md
